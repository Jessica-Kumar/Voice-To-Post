fastapi
uvicorn
pydantic
faiss-cpu
sentence-transformers
python-multipart
httpx
python-dotenv
sqlalchemy
newsapi-python
cryptography
huggingface_hub
dateparser==1.2.0
apscheduler==3.10.4
tweepy>=4.14.0
pytz
PyPDF2

# Direct Google GenAI SDK — replaces langchain-google-genai to avoid version conflicts
# with newer models like gemini-2.5-flash
google-generativeai>=0.8.0

# Keep langchain core for PromptTemplate only (used nowhere now, but safe to keep)
# langchain-google-genai is REMOVED — it was causing silent failures with gemini-2.5-flash